<%= render 'layouts/top_nav_bar' %>

<h1>Inquiries</h1>

<table>
  <!----<thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>--->

  <tr>
    <th>ID</th>
    <th>Created By</th>
    <th>Subject</th>
    <th>Message</th>
  </tr>

    <% @inquiries.each do |inquiry| %>
      <tr>
        <td><%= inquiry.id %></td>
        <!--<td><#% created_by = User.find(inquiry.user_id) %><#%= created_by.name %></td>-->
        <td><%= inquiry.subject %></td>
        <td><%= inquiry.message %></td>
        <td><%= link_to 'Show', inquiry %></td>
        <td><%= link_to 'Edit', edit_inquiry_path(inquiry) %></td>
        <td><%= link_to 'Destroy', inquiry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
 <!-- </tbody>-->
</table>

<br>

<%= link_to 'New Inquiry', new_inquiry_path %>
